import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader.js';

// const loader = new GLTFLoader();
// let pathModel;
// loader.load('images/path.glb', function (gltf) {
//     pathModel = gltf.scene
//     Scene.add(gltf.scene);

//     model.scale.set(1, 1, 1);  // Scale model
//     model.position.set(0, 0, 0);  // Position model
// });

// const startButton = document.getElementById('startButton');

// startButton.addEventListener('click', () => {
//   start = true
//   overlay.classList.add('hidden'); // Hide the overlay
//   canvas.style.zIndex = 1; // Make sure the canvas is above any other content
// });