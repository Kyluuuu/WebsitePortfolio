{
  "name": "webportfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "vite": "^5.4.10",
    "vite-plugin-static-copy": "^2.1.0"
  },
  "dependencies": {
    "three": "^0.170.0",
    "webportfolio": "file:"
  }
}
